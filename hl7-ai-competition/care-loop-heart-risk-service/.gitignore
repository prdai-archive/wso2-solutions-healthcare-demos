.venv/
__pycache__/
*.pyc
.ruff_cache/
.pytest_cache/
.env
# Kaggle dataset is not redistributed; download it locally (see README).
training/data/
# Regenerated by `make train`; the served ONNX graph is committed instead.
models/*.joblib
