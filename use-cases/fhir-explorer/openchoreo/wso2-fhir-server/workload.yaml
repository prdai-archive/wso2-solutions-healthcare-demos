apiVersion: openchoreo.dev/v1alpha1
kind: Workload
metadata:
  name: wso2-fhir-server-workload
  namespace: default
spec:
  owner:
    projectName: fhir-canvas-explorer
    componentName: wso2-fhir-server
  endpoints:
    http:
      type: HTTP
      port: 9090
      visibility: [project]
  dependencies:
    resources:
      - ref: fhir-postgres
        envBindings:
          host: DB_HOST
          port: DB_PORT
          username: DB_USER
          password: DB_PASSWORD
          database: DB_NAME
  container:
    # Bootstrap only — the build pipeline overwrites this on the first WorkflowRun.
    image: fhir-canvas-explorer/wso2-fhir-server:bootstrap
    env:
      - key: SERVER_PORT
        value: "9090"
      - key: BASE_URL
        value: http://wso2-fhir-server:9090/fhir/r4
      - key: LOG_LEVEL
        value: info
      - key: FHIR_CREATE_TABLES
        value: "true"
